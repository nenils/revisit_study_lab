services:
    study:
        ports:
          - "0.0.0.0:${STUDY_HTTP_PORT:-80}:80"
          - "0.0.0.0:${STUDY_HTTPS_PORT:-443}:443"
    volumes:
      - ./docker/nginx/https.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro


